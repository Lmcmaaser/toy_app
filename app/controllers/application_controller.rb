# base class for controllers provided by the Rails library Action Pack
# gives controllers the ability to manipulate model objects, filter inbound HTTP requests, and render views as HTML
# rules defined in the ApplicationController automatically apply to every action in the application.

class ApplicationController < ActionController::Base
end
